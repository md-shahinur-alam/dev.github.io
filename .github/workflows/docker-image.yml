name: Docker Image CI

on:
  push:
    branches: [ "https/shahinuralam.com" ]
  pull_request:
    branches: [ "https/shahinuralam.com" ]

jobs:
Wwb development
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
